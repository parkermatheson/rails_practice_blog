<%= form_with(model: homework_blog, local: true) do |form| %>
  <% if homework_blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(homework_blog.errors.count, "error") %> prohibited this homework_blog from being saved:</h2>

      <ul>
      <% homework_blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :homework_blog_title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :homework_blog_body %>
  </div>

  <div class="field">
    <%= form.label :published %>
    <%= form.check_box :published, id: :homework_blog_published %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
